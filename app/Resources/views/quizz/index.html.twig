{% extends 'layout.html.twig' %}
{% block pageContent %}
    <div class="quizz">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <h3 class="panel-title">{{ question.title }}</h3>
            </div>
            <div class="panel-body">
                {% for answer in question.QuizzAnswers %}
                    <a id="{{ answer.id }}" data-toggle="modal" href="#modal-{{ answer.id }}">
                        <div class="col-md-5 answer">
                            {{ answer.choice }}
                        </div>
                    </a>
                    <div class="modal fade" id="modal-{{ answer.id }}">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
                                        &times;
                                    </button>
                                    <h4 class="modal-title">Réponse</h4>
                                </div>
                                <div class="modal-body">
                                    Mon Contenu
                                </div>
                                <div class="modal-footer">
                                    <a href="#" type="button" class="btn btn-primary">Suivant</a>
                                </div>
                            </div><!-- /.modal-content -->
                        </div><!-- /.modal-dialog -->
                    </div><!-- /.modal -->
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}