{% extends 'base.html.twig' %}

{% block layout %}
    <h1>Resourcetheme creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('resourcetheme_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
