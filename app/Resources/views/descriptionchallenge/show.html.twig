{% extends 'base.html.twig' %}

{% block body %}
    <h1>Descriptionchallenge</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ descriptionChallenge.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ descriptionChallenge.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ descriptionChallenge.content }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('descriptionchallenge_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('descriptionchallenge_edit', { 'id': descriptionChallenge.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
