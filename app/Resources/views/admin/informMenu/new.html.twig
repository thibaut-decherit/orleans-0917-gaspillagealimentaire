{% extends 'admin/layoutAdmin.html.twig' %}

{% block pageContent %}
    <div class="panel panel-default admin">
        <div class="panel-heading">
            <h1 class="panel-title text-info text-center">S'informer - Ajouter une information</h1>
        </div>
        <div class="panel-body">
            <div class="row">
                <a href="{{ path('admin_inform_menu_index') }}" class="btn_admin btn btn-default pull-right">
                    <span class="glyphicon glyphicon-chevron-left"></span> Retour à la liste
                </a>
            </div>
            {{ form_start(form, {'attr':{'id':'form-submit-button'}}) }}
            {{ form_widget(form) }}
            <button id="button-submit" class="btn_admin btn btn-default" type="submit">
                <span class="glyphicon glyphicon-ok"></span> Valider
            </button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}